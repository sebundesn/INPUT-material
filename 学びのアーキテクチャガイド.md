# **学びのアーキテクチャガイド**

## **1\. 本書の目的と基本思考**

AIがコーディングの大部分を代替・加速する時代において、エンジニアには以下の能力や役割がより強く求められると考えられる。

* 専門性の深掘り: ネットワーク、クラウドインフラ、セキュリティなどの高度な専門知識。  
* ビジネス力: ビジネスモデルやドメインロジックの理解と要件定義能力。  
* 技術とビジネスの融合 (システムアーキテクト / PM): 全体最適を見据えたシステム設計とプロジェクトのマネジメント力。


今回はアプリ開発の概要をつかむため、システムアーキテクチャを学んだ。

---

## **2\. 非機能要件の設計指標**

* パフォーマンス:  
  * API応答時間目標（例: 95thパーセンタイルで200ms以内など）。  
  * レスポンスサイズ最小化、通信回数の削減。  
      
* 可用性・運用性:  
  * 稼働率目標（例: 99.9% 以上）。  
  * 無停止デプロイメントの推進。  
      
* 拡張性:  
  * ステートレスなアプリケーション設計による水平スケール（Scale-Out）対応。  
  * トラフィックに応じたオートスケーリング構成。  
      
* セキュリティ:  
  * OWASP Top 10 に対する対策の全量実施。  
  * 最小権限の原則（PoLP: Principle of Least Privilege）の適用。  
    

---

## **3\. 技術選定のガイドライン**

| レイヤー | コンポーネント役割 | 評価・選定の観点 |
| :---- | :---- | :---- |
| Frontend | ユーザーインターフェース / クライアント状態管理 | レンダリング方式の柔軟性、型安全性、エコシステムおよびコンポーネントライブラリの充実度 |
| Backend | API提供 / ビジネスロジック処理 | 並列処理性能、型システムの堅牢性、開発エコシステム、チームの習熟度および保守性 |
| Datastore (RDB / NoSQL) | 永続データの保持・整合性管理 | ACID特性の要否、データ構造の柔軟性、クエリの複雑さ、読み書き性能およびスケール方式 |
| Cache / Broker | 高速レスポンス / 非同期処理の仲介 | セッション/キャッシュ共有の要否、非同期メッセージング・ジョブキューの信頼性 |
| Infrastructure / Compute | 実行環境 / 運用の自動化 | コンテナオーケストレーションの容易さ、Ops負荷（フルマネージド性）、オートスケーリング機能 |
| CI/CD | パイプライン / 品質検証自動化 | 静的解析（Lint/TypeCheck）、自動テスト、ビルド・デプロイの自動化およびセキュリティスキャン連携 |

---

## 4\. 汎用システム構成パターン

特定のクラウドベンダーに依存しない、責任境界を明確にしたコンテナ/マイクロサービス指向の標準構成

```
[ Client / Browser / Mobile ]
            │
            ▼
[ Edge / CDN / Load Balancer ]
            │
            ▼
[ Compute / API Gateway / App Server ]
    ├──► [ Cache / Session Store ]
    ├──► [ Primary Datastore (RDB / NoSQL) ]
    └──► [ Blob / Object Storage ]
```

---

## **5\. アプリケーション層構造方針**

アーキテクチャは「関心の分離」を基本とし、ドメイン（ビジネスルール）が外部技術（DBやWebフレームワーク）に依存しない単一方向の依存関係を保つ

```
src/
├── domain/          # ビジネスルール・エンティティ（最内層：外部へ依存しない）
│   ├── models/
│   └── interfaces/  # データアクセスや外部連携の抽象インターフェース
├── usecase/         # アプリケーション固有の業務フロー・ユースケース制御
├── infrastructure/  # 外部依存の実装（DBアクセス、外部API、サードパーティSDK）
├── presentation/    # 外部入出力の処理（HTTP/gRPC/CLIハンドラ、ミドルウェア）
└── config/          # 環境変数・アプリケーション設定・DI初期化
```

---

## **6\. コーディング・設計規約**

### **6.1 命名規則**

プロジェクトで使用するプログラミング言語やフレームワークの標準イディオムに準拠することを最優先としつつ、システム全体での一貫性を確保する。

* モジュール・ファイル名:　 システム環境やファイルシステム（大文字小文字の区別等）に依存しにくい一貫したケースを採用する。  
    
* 型・クラス・インターフェース:　 概念や構造体を明示するため、先頭大文字を使用

* 変数・関数・メソッド:　 動作や状態を表す名称とし、言語標準の推奨表記に統一  
    
* 定数・環境変数:　 不変の値であることを明示するため、強調表記  
    
* データベースオブジェクト: 　テーブル名やカラム名は、使用するRDBMSの推奨表記に従い統一

### **6.2 設計・実装方針**

* **依存性の注入**:　 プレゼンテーション層やユースケース層は具象実装ではなくインターフェースに依存させる（疎結合の維持）。  
    
* **アーリーリターン**:　 ガード節を活用してネストを深めず、正常系以外の処理は早期にリターン


---

## **7\. データベース設計原則**

* **正規化方針**: 　原則として第三正規形まで正規化を実施。集計パフォーマンス等が問題となる場合のみ慎重に非正規化を検討。  
    
* **標準カラム**: 　すべての主要テーブルに `id`, `created_at`, `updated_at` を保持  
    
* **削除戦略**: 論理削除（`deleted_at`）の無闇な導入は避け、物理削除または状態ステータス（`status = 'archived'`）を優先検討  
    
* **マイグレーション**: 　DBスキーマ変更はすべてコード（IaC/マイグレーションツール）で管理し、破壊的変更はExpand and Contractパターン等で段階的に行う。


---

## **8\. セキュリティ設計原則**

* **認証・認可**:  ステートレスな JWT / OAuth 2.0 ベースの認証構成。リソースアクセス時にはロールベースアクセス制御を検証。  
    
* **データ保護**:  通信（TLS）および保存データ（At-rest）の暗号化。パスワードは安全なアルゴリズムでハッシュ化。  
    
* **脆弱性対策**:  SQLインジェクション、XSS、CSRF対策の徹底。依存ライブラリの脆弱性スキャンを自動化。

## **9\. テスト・品質保証戦略**

テストピラミッドに基づき、費用対効果の高いテスト自動化を推進します。

* **E2E Tests**: 主要な重要ユーザーシナリオのみを自動化。  
* **Integration Tests**: APIレスポンスおよびデータベース結合テスト。  
* **Unit Tests**: ドメインロジック・コア機能の単体テスト。  
* **テスト関数名**: `Test<機能名>_<条件>_<期待結果>`（例: `TestGetUser_WhenUserNotFound_ReturnsNotFoundError`）

## **10\. 保守性・可観測性・拡張性方針**

* **フィーチャーフラグ**:  新機能の段階的デプロイや緊急時の機能オフのための仕組みを設ける。  
* **構造化ログと分散トレース**:  ログは JSON 形式の構造化ログとして標準出力に出力し、集約基盤へ送信。リクエストID（Trace ID）を全体で伝搬させる。  
    
* **非同期処理の分離**:  重い処理（メール送信、画像処理等）はメッセージキューを経由して非同期ワーカープロセスで実行する。

